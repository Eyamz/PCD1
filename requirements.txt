# Tunisian Proverbs — Web Application
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Vector store + embeddings
# FIX: chromadb 0.4.x uses the new PersistentClient API
chromadb>=0.4.24
sentence-transformers>=2.6

# LLM inference (Phi-2)
torch>=2.1
torchvision>=0.16
transformers>=4.39
accelerate>=0.27
tokenizers>=0.15
safetensors>=0.4
einops>=0.7

# Image generation (SDXL)
diffusers>=0.25.0
pillow>=10.0

# RAG Pipeline (FAISS + OpenRouter)
langchain>=0.1.0
langchain-community>=0.0.10
langchain-huggingface>=0.0.1
faiss-cpu>=1.7.4
datasets>=2.14.0
pandas>=2.1.0
requests>=2.31.0
python-dotenv>=1.0.0

# Audio narration (Darja TTS)
gtts>=2.3.0

# Legacy (Llama 2 quantization - no longer needed but kept for reference)
# bitsandbytes>=0.41.0